# Trained Model & Testing Code



The arguments in each model slightly differ. If you want to run the test for a specific model, use the corresponding test code and set up the directory correctly. Make sure to have pytorch_dataset.py, best_ebsd_strain_model.pth (check the version), and ebsd_fcc_fe.h5 in the directory.
