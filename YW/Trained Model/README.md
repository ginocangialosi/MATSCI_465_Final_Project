# Trained Model & Testing Code (Latest model: v4 - 3/9/26 15:29)


- The notebooks primarily run in Google Colab.
- For model training, make sure to have the ebsd_fcc_fe.h5 file (from the dataset creation code in VR's folder) in the directory and change the path in the code.
- For model testing, the arguments taken by each model slightly differ. If you want to run the test for a specific model, use the corresponding test code in the folder and set up the path correctly. Make sure to have pytorch_dataset.py, best_ebsd_strain_model.pth (check the version, all of them have the same file name), and ebsd_fcc_fe.h5 in the directory.
